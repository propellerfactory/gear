import Colors from 'gear/colors'
import { IToggleColors } from 'gear/Toggle/Variants/colors'

const colors: IToggleColors = {
    background: {
        on: Colors.deep,
        off: Colors.granite
    },
    thumb: Colors.white,
    focus: Colors.pink.default
}

export default colors
